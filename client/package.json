{
  "name": "zebulon-frontend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "start": "vite preview",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.3.1",
    "lucide-react": "^0.539.0",
    "fast-xml-parser": "^5.2.5"
  },
  "devDependencies": {
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "typescript": "^5.9.2",
    "vite": "^4.0.0"
  }
}
// Netlify build instructions:
// 1. Run `npm install` to install dependencies.
// 2. Run `npm run build` to compile frontend to dist/.
// 3. Run `npm start` to serve the compiled frontend instantly.
// Only runtime dependencies are included in production. Dev dependencies are used for build only.
